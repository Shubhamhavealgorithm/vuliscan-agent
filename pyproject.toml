[tool.poetry]
name = "vuliscan-agent"
version = "0.1.0"
description = "A brief description of your package"
authors = ["ShubhamD <your_email@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry-core.masonry.api"